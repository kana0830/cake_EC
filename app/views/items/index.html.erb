<!-- 訳あってindexとshowを取り替えました -->

<div class="container" style="margin-top:20px;">
<div class="row">

	<nav class="col-lg-2" style="background-color: #f0f8ff;">
		<table>
			<thead>
				<tr>
					<th>ジャンル検索</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>ケーキ</td>
				</tr>
				<tr>
					<td>焼き菓子</td>
				</tr>
				<tr>
					<td>プリン</td>
				</tr>
				<tr>
					<td>キャンディ</td>
				</tr>
			</tbody>
		</table>
	</nav>


	<main class="col-lg-10" style="background-color: #f0f0f0">
		<div class="row">
			<h5>商品一覧(全xxxx件)</h5>
		</div>
		
		<div class="row">
			<div class="col-lg-3">	
					<!-- ここにeach文を差し込めばOK -->

<!-- 
				<%= @items.each do |item| %>
					<%#= link_to item_path(item.id) %>
						<%= attachment_image_tag item, :image, format: 'jpeg', size: "300x200", fallback: "no_image.jpg" %>
					<%# end %>
					<%= item.name %>
					<%= item.price %>
				<% end %>-->



				<% @items.each do |item| %>
              <%= link_to item_path(item) do %>
                <%= attachment_image_tag item, :image, format: 'jpeg', size: "300x200", fallback: "no_image.jpg" %>
              <% end %>
							<%= item.name %>
              ¥ <%= item.price %>
        <% end %>
			</div>
		</div>		<!-- rowの閉じタグ -->
</div>		<!-- containerの閉じタグ -->
